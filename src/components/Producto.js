import React from 'react'
import {useParams} from "react-router-dom";

const Producto = () => (
    //let {nombre} = useParams();
    <div className="familias"> Producto</div>
)

export default Producto